import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  flatListStyle: {
    width: '100%',
  },

  loaderWrapper: {
    position: 'absolute',
    top: 20,
  },
});

export default styles;
